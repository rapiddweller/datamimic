<setup rngSeed="1">
    <!-- C-1: iterationSelector requires source (DM214 warning) -->
    <generate name="test" count="1" target="ConsoleExporter">
        <variable name="bad_var" iterationSelector="SELECT * FROM users"/>
        <key name="id" type="int" min="1" max="100"/>
    </generate>
</setup>
