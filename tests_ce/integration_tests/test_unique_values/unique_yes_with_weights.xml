<setup rngSeed="5">
    <!-- wrong/loose bool: unique="yes" coerces to True -> the weights conflict MUST still fire. -->
    <generate name="x" count="2" target="">
        <key name="code" values="'A','B','C'" weights="1,2,3" unique="yes"/>
    </generate>
</setup>
