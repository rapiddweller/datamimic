<setup rngSeed="5">
    <!-- None among the values, plus a duplicate None -> deduped to {None, A, B} = 3 distinct. -->
    <generate name="people" count="3" target="">
        <key name="code" values="None,'A',None,'B'" unique="true"/>
    </generate>
</setup>
