<setup rngSeed="13">
    <!-- 3 values but 2 weights on <variable> -> must fail when the variable is evaluated. -->
    <generate name="x" count="1" target="">
        <variable name="grade" values="'A','B','C'" weights="0.1, 0.3"/>
        <key name="result" script="grade"/>
    </generate>
</setup>
