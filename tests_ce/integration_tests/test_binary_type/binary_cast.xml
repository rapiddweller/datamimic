<setup>
    <!-- type="binary" casts a str script result to utf-8 bytes (like int()/str() for other types) -->
    <generate name="g" count="1" target="">
        <key name="from_str" type="binary" script="'hello'"/>
    </generate>
</setup>
