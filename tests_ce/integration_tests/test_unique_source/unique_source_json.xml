<setup rngSeed="9">
    <!-- unique distinct rows from a json source (random order, no replacement). -->
    <generate name="people" count="5" target="">
        <variable name="row" source="codes.json" unique="true"/>
        <key name="code" script="row.code"/>
    </generate>
</setup>
