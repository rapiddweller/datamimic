<setup rngSeed="9">
    <!-- unique + cyclic is contradictory -> validation error. -->
    <generate name="x" count="3" target="">
        <variable name="row" source="codes.csv" unique="true" cyclic="true"/>
        <key name="code" script="row.code"/>
    </generate>
</setup>
