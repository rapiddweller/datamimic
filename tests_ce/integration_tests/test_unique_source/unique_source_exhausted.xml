<setup rngSeed="9">
    <!-- 8 rows but only 5 distinct source values -> strict error (no silent cap). -->
    <generate name="x" count="8" target="">
        <variable name="row" source="codes.csv" unique="true"/>
        <key name="code" script="row.code"/>
    </generate>
</setup>
